# TipRouter — Contract Interface

> **Status: placeholder — generated on Day 6 (M3 → M4 integration readiness).**

This document will contain the full, generated reference for every public
function of the `TipRouter` contract:

- Full function signature with typed parameters
- Description of behavior
- Authorization requirements (who must sign, and how)
- Preconditions for success
- Error cases and their meaning
- Emitted events (`TipSent`: `tipper`, `creator`, `amount`, `asset`,
  `fee_amount`, timestamp) and their types
- Example `soroban contract invoke` invocations per function

No contract logic exists yet (Day 1 scaffold), so there is nothing to document
until M1. Interface-freeze happens in M5 ahead of audit; see `PLAN.md`.